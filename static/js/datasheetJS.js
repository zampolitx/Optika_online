? - /*Тернарный оператор. Вычисляет условие, если оно истинно
 возвращает значение 1, иначе значение 2
 let user = (условие) ? значение1 : значение2*/
**********************************************************
?? - /*Возвращает первое значение, которое было определено 
, иначе последнее. Приоритет 5. Если совместно с && или || 
нужны скобки. пример let user = '0' ?? 'Ivan' вернет 0*/